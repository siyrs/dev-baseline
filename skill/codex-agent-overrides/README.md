# Codex Agent Overrides

Codex project overlays start from the common agents in `../agents`.

Only keep files here when Codex needs a different role prompt or an extra Codex-specific role. Do not duplicate common agent files that are identical to `../agents`.

# Sprint Mode

Use sprint mode to group multiple task workspaces into one iteration.

## Command

```bash
bash shared/scripts/create-sprint-workspace.sh <version> <sprint-name>
```

## Output

```text
docs/sprints/YYYYMMDD-vX.Y.Z-sprint-slug/SPRINT.md
```

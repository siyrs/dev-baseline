# Release Train Mode

Use release train mode to coordinate accepted tasks into a release candidate.

## Command

```bash
bash shared/scripts/create-release-workspace.sh <version> <release-name>
```

## Output

```text
docs/releases/YYYYMMDD-vX.Y.Z-release-slug/RELEASE_TRAIN.md
```

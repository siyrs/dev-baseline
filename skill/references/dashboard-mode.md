# Dashboard Mode

Use this mode to generate a visual overview of task workspaces.

Command:

```bash
bash shared/scripts/generate-task-dashboard.sh
```

Output:

```text
docs/tasks/dashboard.html
```

name: Validate Dev Baseline Skill

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  validate-skill:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Validate skill package
        run: bash scripts/validate-skill.sh
